import * as types from '../constants/actionTypes';

const initialState = {
    user: null
};

export default function login(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}